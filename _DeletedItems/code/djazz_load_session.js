autowatch = 1;

function load(foldername)
{
    var f = new Folder(foldername);
    while (!f.end)
    {
        switch (f.extension)
        {
            case '.navigate':
                
                break;
            case '.midi_out':
                break;
        }
    }
}